# VAL-RE001-RESOURCES: Candidate costs and bounded resource ownership are visible

Surface: browser.
Needs: Bookhand `1743074` with official Foliate `78914aef4466eb960965702401634c2cb348e9b1`, the exact candidate production build, and the same fixture/browser/runtime.
Behavior: The report compares installed/archive and production asset sizes, first readable section, page-turn and reflow settle observations, and a fixed probe of 20 open/navigate-five-sections/close cycles plus 100 turns in one open book. Frame count returns to zero after each close. Ordinary operation has one visible authoritative `foliate-view`/iframe; bounded stalled-navigation recovery may temporarily add exactly one hidden non-interactive speculative view/iframe, after which counts return to one on either success or failure. Candidate-created object URLs, workers, canvases, observers, and listeners return to the baseline post-close count. If source inspection proves the candidate creates none in a category, record that with runtime zero confirmation. Unsupported heap or ownership metrics remain blocked.
Evidence: Baseline/candidate manifests, raw sample series and method, all 20 cycle rows and 100-turn samples, frame/view/object-URL/worker/canvas/observer/listener counts with probe method, source-backed no-creation claims where applicable, and explicit blocked long-session/heap metrics.
Fail: Fewer cycles are silently used, more than one speculative view appears, transient recovery counts do not return to one, post-close counts do not return to zero/baseline, counts trend upward without explanation, a required ownership probe is unavailable but passed, single noisy timings become performance claims, internal fork counters are treated as stable Bookhand APIs, or unavailable memory evidence is called passed.
Oracle: Same-machine differential run; no absolute performance claim.
