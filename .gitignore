# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
.wrangler/
.playwright-mcp/
playwright-report/
test-results/
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Impeccable session artifacts. `.impeccable/config.json` and `design.json` are
# shared project decisions and stay tracked; everything below is per-run state.
.impeccable/config.local.json
.impeccable/mocks/
.impeccable/review/
.impeccable/critique/
.impeccable/build/
.impeccable/live/sessions/
.impeccable/hook.cache.json

# Validation evidence: screenshots and traces kept as evidence for a decision.
artifacts/

# Locally installed skill packs. Repository-owned skills that are already
# tracked remain tracked; local package-manager projections and their lockfile
# should not become product source by accident.
.agents/skills/
.claude/skills/
skills-lock.json
